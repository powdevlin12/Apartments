export type ComponentType = 'normal' | 'pro';

const C_PRO: ComponentType = 'pro';
const C_NORMAL: ComponentType = 'normal';

export const TTypeComponent = Object.freeze({
	PRO: C_PRO,
	NORMAL: C_NORMAL,
});
